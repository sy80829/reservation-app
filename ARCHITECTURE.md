# 設計思想・アーキテクチャ判断の記録

このドキュメントは、「何を作ったか」（→ [仕様書.md](./仕様書.md)）ではなく、**なぜそう設計したか**をまとめたものです。

## 目次

- [0. 制作の背景・技術選定](#0-制作の背景技術選定)
- [1. Server ActionsとAPI Routeの使い分け](#1-server-actionsとapi-routeの使い分け)
- [2. 予約作成フローのクエリパラメータ廃止](#2-予約作成フローのクエリパラメータ廃止)
- [3. RLSとアプリ側の所有者チェックによる多重防御](#3-rlsとアプリ側の所有者チェックによる多重防御)
- [4. 予約の二重防止は複数層で行っている](#4-予約の二重防止は複数層で行っている)
- [5. loading.tsxとコンポーネント内スケルトンの使い分け](#5-loadingtsxとコンポーネント内スケルトンの使い分け)
- [6. lintルールを理由付きで部分的に無効化する方針](#6-lintルールを理由付きで部分的に無効化する方針)

## 0. 制作の背景・技術選定

### 背景

本アプリを制作した背景は、以下の通りである。

- 予約の重複防止や空き時間管理など、業務システムで扱われる処理を想定し、設計・実装を行うため
- Next.js・React・TypeScriptは今後さらに伸ばしていきたいモダンなWeb技術であり、実践的な題材を通して深く理解するため

### 技術選定

本アプリは、モダンなフロントエンド開発の実践を目的として、React（Next.js）/TypeScript/Supabaseを用いて開発している。

**React**
再利用性の高いUI設計や状態管理を実現するため選定した。

**Next.js**
Route HandlerやServer Actionsを活用することで、APIの実装やデータ更新処理をフロントエンドと近い位置で記述でき、フロントエンド・バックエンドの両方を1つのプロジェクトでシンプルに構成できるため選定した。

**TypeScript**
型安全性を担保しバグを防げるため選定した。

**Supabase**
データベースおよびAPI機能（認証・RLSなど）を一体的に提供しており、バックエンドの構築コストを抑えつつ、シンプルな構成でデータ管理ができるため選定した。

## 1. Server ActionsとAPI Routeの使い分け

**判断**：予約の作成・変更・キャンセルなど**更新系はServer Actions**、コース一覧の取得など**参照系はAPI Route**で実装している。

**理由**：更新系はフォーム送信・ボタン操作から直接呼び出すことが多く、Server Actionsの方がクライアント側のコードがシンプルになる。一方、参照系はクライアントコンポーネントから複数回・任意のタイミングで呼び出すことが多いため、通常のfetchで叩けるAPI Routeの方が扱いやすい。

## 2. 予約作成フローのクエリパラメータ廃止

**判断**：新規予約の選択内容（コースID・日時など）を、URLのクエリパラメータではなくJotai Atom（`sessionStorage`永続化）で管理している。

**理由**：もともとはURLに直接載せていたが、以下の理由で置き換えた。

- コースIDなどをURLに露出させたくない
- 一方で、会員未登録ユーザーが登録画面を経由しても選択内容を失いたくない（リロード耐性が必要）
- `localStorage`だと「ブラウザを閉じても消えない」状態が気持ち悪く、`sessionStorage`が「タブを閉じたら消える・でも画面遷移では残る」というちょうど良い性質を持っていた

予約確定ボタンを押した時点でドラフトをクリアし、古い選択内容が後から復活しないようにしている。

## 3. RLSとアプリ側の所有者チェックによる多重防御

**判断**：予約データへのアクセス制御を、SupabaseのRLS（Row Level Security）とアプリ側のコード（`getOwnReservation`）の**両方**で行っている。

**背景**：開発中に「予約完了画面で他人の予約IDを直接指定すると詳細が見えてしまう」というIDOR脆弱性を発見した。原因は`user_id`での絞り込みを1画面だけ書き忘れていたこと。

**理由**：RLSは「最後の砦」として機能するが、空き状況カレンダーの計算には全ユーザーの予約データを読む必要があり、SELECTを完全に本人限定にはできない構造になっている。そのため、個々の画面での「本人のデータか」のチェックはアプリ側の責務として残る。同じロジックを画面ごとに書くと今回のような書き忘れが再発するため、`getOwnReservation`という共通関数に閉じ込め、書き忘れそのものを起きにくくした。

## 4. 予約の二重防止は複数層で行っている

**判断**：予約の重複防止を、①DB関数（RPC）内のロジック、②DBのEXCLUDE制約、③楽観ロック（`version`列）の3段階で行っている。

**理由**：フロントの空き状況カレンダーはあくまで「見た目のプレビュー」であり、実際に予約が成立するかどうかの最終判定はDB側のRPC関数（`create_reservation`・`update_reservation`）が担っている。実際、開発中にフロントのカレンダー計算にバグ（指名なし予約で空き状況が正しく表示されない）が見つかったが、これは表示上の不具合に留まり、二重予約のようなデータ不整合は一度も起きなかった。「大事な判定はDBに寄せる」という設計のおかげで、フロント側のバグが致命的にならずに済んだ。

RPC関数本体の定義は[supabase/migrations/20260722000000_reservation_functions.sql](./supabase/migrations/20260722000000_reservation_functions.sql)を参照。

## 5. loading.tsxとコンポーネント内スケルトンの使い分け

**判断**：Next.jsの`loading.tsx`（サーバー側のSuspense）と、コンポーネント内で管理するローディングstate（クライアント側のフェッチ待ち）を両方使っている。

**理由**：`loading.tsx`はサーバーコンポーネントの処理中にしか効かない。`Top.tsx`のようにクライアント側で`useEffect`によるデータ取得を行う画面では、`loading.tsx`が役目を終えた後に別の空白時間が発生するため、コンポーネント自身が持つ状態でカバーする必要がある。両者の見た目（スケルトンの形）は統一し、画面遷移の途中で見た目が変わる違和感をなくしている。

## 6. lintルールを理由付きで部分的に無効化する方針

**判断**：`react-hooks/set-state-in-effect`などのルールを、機械的に全て解消するのではなく、コード上で理由をコメントした上で個別に無効化している箇所がある。

**理由**：このルールは「データ取得後（await後）に呼ばれる非同期のsetState」まで一律に警告するため、正当な使い方まで拾ってしまうケースがあった。実際、fetch関数を`useEffect`の中に移動しただけで警告そのものが解消されたケースが大半で、本当に無効化が必要だったのは「マウント時の値だけを使いたい」という1箇所だけだった。ルールに機械的に従うのではなく、指摘の妥当性を都度確認する方針を取っている。
