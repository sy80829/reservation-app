# 予約アプリ 仕様書

## 1. 概要
美容室（ヘアサロン等）向けの予約管理Webアプリケーション。会員（顧客）がGoogleアカウントでログインし、コース・担当スタイリスト・日時を選んでオンライン予約・変更・キャンセルができる。

## 2. 技術スタック
| 分類 | 使用技術 |
|---|---|
| フレームワーク | Next.js 16（App Router）, React 19 |
| データベース / BaaS | Supabase（PostgreSQL） |
| 認証 | Supabase Auth（Googleログイン） |
| 状態管理 | Jotai |
| フォーム | ネイティブHTMLフォーム + Server Actions、zod（サーバー側バリデーション） |
| UI | Tailwind CSS 4, Radix UI / shadcn/ui, lucide-react |
| 日付選択UI | react-day-picker（shadcn/uiのCalendar） |
| メール送信 | Resend + React Email |

## 3. 画面一覧
| 画面 | パス | 内容 |
|---|---|---|
| ログイン | `/login` | Googleログイン |
| 会員登録 | `/registerUser` | 初回ログイン時に氏名・電話番号を登録 |
| トップ | `/top` | ログイン後のトップ画面（予約状況カレンダー） |
| 新規予約 | `/reservation` | コース・スタイリスト・日時を選択 |
| 予約確認 | `/reservation/confirm` | 予約内容の確認 |
| 予約完了 | `/reservationComplete/[id]` | 予約完了通知 |
| 予約変更 | `/reservation/edit/[id]` | 既存予約の変更 |
| キャンセル完了 | `/cancelComplete` | キャンセル完了通知（キャンセル操作自体は予約確認ページのモーダルから実行） |

## 4. 主な機能
- **予約作成**：コース・スタイリストの実在チェック、空き時間の重複チェック（DB側RPCで二重予約防止）、予約完了メール送信
- **予約変更**：楽観ロック（`version`列）による競合検知、変更完了メール送信
- **予約キャンセル**：楽観ロックによる競合検知、キャンセル完了メール送信
- **空き枠表示**：10:00〜19:00を30分単位で空き状況を算出し、コースの所要時間分連続して空いている枠のみ予約可能と判定。スタイリスト未指定時は全スタイリストの空き状況をマージして判定
- **日付ジャンプ**：カレンダーUIから日付を選ぶと、該当日の空き状況カードまでカルーセルがスクロールする（`/top`）
- **会員登録**：初回ログイン時に氏名・電話番号を登録。zodによるサーバー側バリデーション（未入力・電話番号形式）あり
- **エラーハンドリング**：予期しない例外は`error.tsx`、存在しないURLは`not-found.tsx`でアプリ全体を共通カバー。予約重複などの想定内エラーはクエリパラメータ（`?error=xxx`）で各画面にinline表示

## 5. データベース（Supabase / PostgreSQL）
| テーブル | 内容 |
|---|---|
| `users` | 会員情報（氏名、電話番号、退会フラグ） |
| `courses` | 施術コース（名称、所要時間、料金） |
| `stylists` | 担当スタイリスト（名前、プロフィール、画像） |
| `reservations` | 予約情報（会員・コース・スタイリスト・予約日時、キャンセル情報、楽観ロック用バージョン） |

DB関数（RPC）：`create_reservation`（予約作成・重複チェック）、`update_reservation`（予約更新）、`get_available_stylists`（空きスタイリスト取得）

## 6. API一覧
| エンドポイント | 内容 |
|---|---|
| `GET /api/calendar` | 指定コース・スタイリストの空き枠カレンダーを取得 |
| `GET /api/course` | コース一覧／単一コース取得 |
| `GET /api/stylist` | スタイリスト一覧／単一スタイリスト取得 |

予約の作成・変更・キャンセルはServer Actionsで実装（APIルートは参照系のみ）。

## 7. 認証・認可
- ミドルウェアで全リクエストを検証し、未ログイン時は `/login` へリダイレクト
- ログイン済みでも会員未登録の場合は `/registerUser` へ強制遷移
- ログイン方法はGoogle OAuthのみ
- 予約データの参照・更新は所有者（`user_id`）本人のみに制限（`getOwnReservation`で共通化した所有者チェック）
- 管理者機能・ロール別権限は現状なし（一般会員向け機能のみ）
